const all = new N1qlQuery(`select * from :bucket`);
if (true)
    all = 10;
for (var i of all)
    for (var i of res) {
        switch (i) {
            case 'xyz':
                
                break;
        
            default:
                break;
        }
            break;   
    }