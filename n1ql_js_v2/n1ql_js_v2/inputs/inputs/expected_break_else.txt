if(res.isInstance){
    res.x = res.iter(function(row) {
        if(true){
            log(row);
        }
        else {
            return res.stopIter({'code':'break'});
        }
    });
}
else {
    for(var row of res){
    if(true)
        log(row);
    else
        break;
    }
}